export const supportText = `

1.To write a root you need a w()\\ . In the brackets, you can write the 
calculation under the root. 
\n
2.To write a square with more than 2 numbers at the exponent you need {}-Brackets, 
into them you need to write the exponent.

`